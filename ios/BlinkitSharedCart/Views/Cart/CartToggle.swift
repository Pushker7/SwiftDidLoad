// Superseded by the cart picker in CartTabView.swift (multi-cart support).
// Kept as an empty file so the Xcode project reference doesn't dangle;
// delete via Xcode's file navigator (not the filesystem) if you want it fully gone.
